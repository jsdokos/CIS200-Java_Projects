
import java.io.File;
import java.io.IOException;
import java.util.*;

public class test 
{

	public static void main(String[] args) throws IOException 
	{
		
		Scanner s_file = new Scanner(new File("life2.txt"));
		int size1, size2;
		
		// getting the file size
		size1 = Integer.parseInt(s_file.nextLine());
		s_file.nextLine();
		size2 = Integer.parseInt(s_file.nextLine());
		s_file.nextLine();
		
		int[][] cells = new int[size1][size2];
			
		String line;
		//translate the file to an array
		for (int i = 0; s_file.hasNextLine(); i++)
		{
			line = s_file.nextLine();
			
			if (i > 0)
				System.out.println();
			for(int j = 0; j < size2; j++)
			{
				cells[i][j] = Character.getNumericValue(line.charAt(j));
				System.out.print(cells[i][j]);
			}
		}
		
		
	} 
} 

